{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "mkdir -p data/pgdata && bun index.ts",
    "start": "mkdir -p data/pgdata && bun index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.15",
    "hono": "^4.11.4"
  }
}
